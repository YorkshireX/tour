<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.swufe.tour">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/guzheng"
        android:label="@string/app_name"
        android:roundIcon="@drawable/guzheng"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".firstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".myField" />
        <activity android:name=".place" />
        <activity android:name=".plan" />
        <activity android:name=".planItem" />
        <activity android:name=".tiezi" />
        <activity android:name=".place_item" />
        <activity android:name=".tieziItem" />
    </application>

</manifest>